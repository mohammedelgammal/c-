#ifndef AVLTREE
#define AVLTREE

#include "BinarySearchTree"
#include "BinarySearchTree.cpp"

class AVLTree : public BinarySearchTree
{
public:
    AVLTree(const int value);
    // Methods
    void insert(const int value) override;

private:
    Node *insert(const int value, Node *node);
    int getBalanceFactor(const Node *root) const;
    bool isLeftHeavy() const;
    bool isRightHeavy() const;
};

#endif // AVLTREE