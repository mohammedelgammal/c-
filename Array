#ifndef ARRAY
#define ARRAY

class Array
{
public:
    // Constructors
    Array(const int length);
    ~Array();

    // Methods
    void insert(int number);
    int operator[](const int index) const;

    // Variables
    int length;

private:
    int index = 0;
    int *array;
};

#endif // ARRAY