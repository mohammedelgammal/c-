#ifndef TRIAL_CLASS
#define TRIAL_CLASS

class TrialClass
{
public:
    TrialClass() = default;
    explicit TrialClass(int value);
    int getValue() const; // inline keyword is unnecessary -> By default inline
    void setValue(int value);

private:
    int value = 0;
};

#endif // TRIAL_CLASS